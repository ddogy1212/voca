# VocabWalk AI v0.5 SPEED — GitHub Pages / PWA

## 가장 간단한 배포

1. 이 폴더 안의 **파일 전부**를 새 GitHub 저장소의 `main` 브랜치에 업로드합니다.
2. GitHub 저장소 → **Settings → Pages**.
3. `Build and deployment`의 `Source`를 **GitHub Actions**로 선택합니다.
4. Actions의 `Deploy VocabWalk to GitHub Pages`가 끝나면 Pages 주소가 생깁니다.
5. 그 주소를 휴대폰에서 엽니다.
6. 앱 상단 **📲 설치**를 누르거나 브라우저 메뉴에서 **홈 화면에 추가 / 앱 설치**를 선택합니다.

GitHub Pages 주소 형태:
`https://사용자명.github.io/저장소이름/`

## AI 연결

GitHub Pages는 정적 사이트이므로 OpenAI API 키를 저장소 코드에 넣으면 안 됩니다.

현재 개인 테스트판은:
- 앱에서 처음 한 번 API 키 입력
- 해당 브라우저 localStorage에 저장
- 다음 실행부터 자동 연결

즉 **GitHub 저장소나 HTML/JS 파일에는 API 키가 들어가지 않습니다.**

공개 서비스로 다른 사용자에게 배포할 단계가 오면 OpenAI 키는 Cloudflare Worker / 서버 / 서버리스 함수 등 서버측 비밀 환경변수로 옮겨야 합니다.

## v0.5 SPEED 기능

- 한 번에 이미지 최대 8장 선택
- 데스크톱 드래그앤드롭
- 사이트 OCR 없이 원본 이미지를 OpenAI 이미지 입력으로 직접 분석
- 1차 사진 인식 후 OpenAI Web Search를 강제로 호출해 철자/뜻 검증
- 웹 검증 성공한 결과만 단어장 저장 가능
- 메인 화면의 사진 추가 카드를 제거하고 `오늘의 단어`로 교체
- 오늘의 단어:
  - 현재 단어장과 중복 회피
  - 현재 학습 단어와 연결되는 어휘
  - 한국 수능/모의고사 독해에 유용한 어휘
  - 웹 검색 확인
  - 5개씩 계속 추가 생성
- 즐겨찾기 단어만 시험
- 걷기 암기에서 `앎` 2회 → 자동 시험 졸업
- 보관함에서 `시험 넣기`로 언제든 복귀
- 의미 기반 AI 채점
- 백업/복원
- PWA / 홈 화면 설치
- GitHub Actions 자동 Pages 배포

## 비용 주의

사진 1회 분석은 기본적으로:
1. 이미지 분석 API 호출
2. 웹 검색 검증 API 호출

두 단계이므로 단순 1회 분석보다 API 사용량이 더 큽니다.
`오늘의 단어`도 5개를 새로 가져올 때 웹 검색 도구를 사용합니다.

## 파일 구조

- `index.html`
- `styles.css`
- `app.js`
- `manifest.webmanifest`
- `service-worker.js`
- `.nojekyll`
- `icons/`
- `.github/workflows/pages.yml`

별도 npm / Node.js / 빌드 과정은 없습니다.


## v0.5 SPEED 변경점

- 사진 분석 2회 API 호출 → **이미지 인식 + 웹 검색 검증을 1회 Responses 요청으로 통합**
- `gpt-5.6`(Sol) 대신 사진 작업은 `gpt-5.6-terra`, 추천/채점은 `gpt-5.6-luna` 우선
- 큰 사진은 OpenAI `high` 이미지 처리 범위에 맞게 최대 2,048px / 약 2.5MP 수준으로 선명하게 최적화
  - OCR을 사이트에서 하는 것이 아니라, 전송 크기만 줄인 뒤 AI가 이미지를 직접 읽습니다.
- 429 / 5xx / 네트워크 오류에 자동 재시도 + 지수 백오프
- 요청 시간 제한 추가
- 사진 분석과 오늘의 단어에 **예상 진행률 0~100% 표시**
- 오늘의 단어는 웹 검색 1회로 최대 15개를 미리 받아 캐시하고, 사용자에게 5개씩 보여줌
  - 첫 호출 뒤 `5개 더`는 캐시가 남아 있으면 즉시 표시
- Service Worker 캐시를 v5로 갱신하고 즉시 활성화

### 이미 v0.4를 GitHub Pages에 올린 경우
새 ZIP의 파일들을 기존 저장소에 덮어쓰면 됩니다.
특히 아래 4개는 반드시 교체:
- `index.html`
- `app.js`
- `styles.css`
- `service-worker.js`

GitHub Actions 배포 완료 후 설치된 앱을 완전히 닫았다가 다시 열어주세요.


## v0.5.1 PHOTO FIX

- Android/Galaxy에서 사진을 선택해도 목록에 안 뜨던 버그 수정
  - 원인: 사진 선택 단계에서 이미지 변환을 먼저 수행했고, `renderPhotos()`가 누락된 빌드가 배포됨
- 이제 사진은 선택 즉시 미리보기 목록에 표시
- 실제 리사이즈/전송 준비는 `빠른 AI 인식 + 검색 검증` 버튼을 누를 때 수행
- MIME type이 비어 있는 Android 사진도 파일 확장자로 판별
- `image/*`, JPG/JPEG/PNG/WEBP/HEIC/HEIF 선택 허용
- HEIC/HEIF를 브라우저가 못 읽는 경우 명확한 안내
- `내 답도 정답으로 기억` 버튼과 기능 삭제
- 기존 단어장/즐겨찾기/시험 졸업 데이터 키는 유지


## v0.5.2 GALAXY PHOTO FIX

사진 선택 경로를 다시 작성했습니다.

- 숨겨진 file input을 label로 여는 방식 제거
- 실제 `📷 사진 고르기` 버튼에서 `input.click()` 실행
- 사진 선택 이벤트를 화면에 즉시 표시:
  - `N장 선택 감지됨`
  - `N장 앱에 들어옴 ✅`
- 미리보기는 FileReader를 우선 사용
- 같은 사진을 다시 골라도 change 이벤트가 발생하도록 input 초기화
- JS/CSS에 `?v=052` 캐시 버스터 추가
- Service Worker 등록에도 `?v=052` + `updateViaCache:"none"` 적용
- HTML/JS/CSS는 network-first + no-store로 갱신
- 상단에 `BUILD 052`가 보여야 최신 버전입니다.

### 업데이트 확인법
앱 상단에 `BUILD 052`가 보이면 최신 코드입니다.
보이지 않으면 GitHub Pages가 아직 새 파일을 배포하지 않았거나 설치 앱이 이전 페이지를 보고 있는 것입니다.


## v0.5.3 MANUAL ADD

- 하단 `＋ 추가` 화면에 `직접 단어 넣기` 기능 추가
- 여러 줄 한꺼번에 입력 가능
- 권장 형식:
  - `reinforce = 강화하다`
  - `undermine = 약화시키다`
- 지원 구분자:
  - `=`
  - `:`
  - 탭
  - 쉼표
  - ` - `
- `/` 또는 `;`로 뜻 여러 개 입력 가능
  - `maintain = 유지하다 / 지속하다`
- 출처 직접 입력 가능
- 입력한 단어 전체를 한 번에 즐겨찾기 가능
- 기존 단어와 철자가 같으면 중복 생성 대신 뜻 병합
- 기존 단어장/시험졸업/즐겨찾기 데이터 유지
- BUILD 053 / 캐시 강제 갱신


## v0.5.4 EDIT RESULT

- AI 사진 인식 결과의 각 단어에 작은 `수정` 버튼 추가
- 수정 버튼을 누르면 그 자리에서:
  - 영어 단어/표현
  - 한국어 뜻
  을 직접 수정 가능
- 뜻 여러 개는 `/` 또는 `;`로 구분 가능
- 일반 인식 단어와 `같이 외울 추가 단어` 모두 수정 가능
- 수정 후 `저장`을 누르면 선택 상태를 유지한 채 즉시 결과에 반영
- 이후 `선택한 단어 저장`을 누르면 수정된 값이 단어장에 저장
- BUILD 054 / 캐시 강제 갱신


## v0.5.5 TEST SKIP

- 테스트에 `모르겠음 · 넘기기` 버튼 추가
- 넘기기는 테스트 정답률의 오답으로 계산하지 않음
- 대신 해당 단어에 `skipCount` 누적
- 넘긴 단어는:
  - 취약도 점수 크게 상승
  - 즉시 복습 대상이 됨
  - 걷기 암기 목록 앞쪽에 우선 등장
  - 같은 걷기 암기 세션에서도 최대 2번 추가 등장
- 걷기 암기에서 `앎`을 누르면 skipCount가 1씩 감소해 점차 정상 빈도로 복귀
- 단어 보관함에서 `넘김 N` 횟수 확인 가능
- 기존 단어장/즐겨찾기/시험졸업 데이터 유지
- BUILD 055 / 캐시 강제 갱신


## v0.6 SCORE — 성적 향상 기능 3종

### 1) 내신 변형어 모드
- 홈에서 별도 모드로 진입
- `섞어서 / 동의어 / 반의어 / 파생어`
- 사진 인식·오늘의 단어에 저장된 관계 정보를 문제로 변환
- 오답은 취약도에 반영
- `모르겠음 · 넘기기`는 일반 오답률에는 넣지 않고 복습 우선순위를 올림
- 지문별 단어장에서 특정 자료의 변형어 문제만 실행 가능

### 2) 지문별 단어장
- `sourceLabel` 기준 자동 그룹
- 사진 추가 때 적은 `영어 프린트 3과`, `모의고사 29번` 같은 이름 그대로 묶임
- 그룹마다:
  - 전체 단어 수
  - 위험 단어 수
  - 시험 졸업 수
  - 준비도
- `이 지문만 시험`
- `이 지문 변형어`

### 3) 학습 리포트
- 학습 기록 기반 `시험 준비도 %`
- 시험 졸업 단어 수
- 위험 단어 수
- 최근 7일 학습/추가 단어 수
- 일반 테스트 정답률
- 가장 위험한 단어 TOP 8
- 철자가 비슷해 헷갈릴 가능성이 큰 단어쌍
- 자료별 준비도
- `지금 10분 공부`:
  취약도 순으로 최대 12개를 골라 걷기 암기 집중 세션 생성

> 시험 준비도는 실제 시험 점수 예측이 아니라 앱 안의 정답/오답/넘김/암기 강도/시험 졸업 상태를 조합한 학습용 지표입니다.

- BUILD 060
- 기존 `vw_words_v4` 저장키 유지 → 기존 단어장/즐겨찾기/졸업 기록 보존


## v0.6.1 QUIET REWARDS

공부 흐름을 방해하지 않는 보상 시스템을 추가했습니다.

### 오늘의 봉투
- 홈 화면에 작은 한 줄 카드로만 표시
- 팝업 자동 노출 없음
- 소리/폭죽/강제 애니메이션 없음
- 하루 1회만 획득 가능
- 사용자가 직접 `열기`를 눌러야 표시

### 봉투 해금 조건
현재 단어장 규모에 맞춰 자동 조정:
- 서로 다른 단어 암기 `앎`: 최대 8개
- 서로 다른 단어 일반 테스트 정답: 최대 5개
- 변형어 데이터가 있는 경우 서로 다른 변형 정답: 최대 3개

같은 단어의 `앎` 연타나 같은 문제 반복으로 보상을 파밍하기 어렵게
**고유 단어/문제만 하루 1회 인정**합니다.

### 보상
1. `🧩 컬렉션 조각`
   - 한 시리즈 8조각
   - 밤하늘 기록 / 오로라 기록 / 밤의 도시 / 종이 위의 별자리
   - 홈의 작은 `컬렉션` 버튼에서만 확인
2. `🔐 비밀 카드`
   - 10초 안에 읽을 수 있는 짧은 영어 학습 팁/함정
   - 외부 링크, 피드, 추가 스크롤 콘텐츠 없음

### 설계 원칙
- 학습 기능이 항상 보상보다 위에 있음
- 보상을 받지 않아도 모든 공부 기능 사용 가능
- 보상은 성적/기능 잠금과 연결하지 않음
- 실제 시험 준비도 계산에는 보상 활동을 반영하지 않음

- BUILD 061
- 기존 단어장 및 모든 학습 기록 유지


## v0.6.2 FULL TEST
- 시험졸업한 단어만 제외하고 나머지는 전부 출제
- 즐겨찾기 전용 시험 옵션 제거
- 기존 10문제 제한 제거
- 문제 아래 `다음 시험엔 안 나오게` 버튼 추가
- 해당 버튼을 누르면 즉시 시험졸업 → 이후 시험에서 제외
- 단어 보관함의 `시험 넣기`로 다시 복귀 가능
- 걷기 암기에서 새 단어가 뜨면 자동 발음
- `🔊 발음` 버튼은 다시 듣기용
- 영→한 시험에서 문제 단어 자동 발음
- 한→영 시험에서는 정답 노출 방지를 위해 자동 발음하지 않음
- BUILD 062


## v0.6.3 GOOD ×2 FIX
- `앎` 2회 후 같은 단어가 걷기 암기에서 계속 등장하던 문제 수정
- 원인: `넘김` 가중치로 동일 단어가 현재 암기 큐에 미리 여러 번 복제될 수 있었음
- `앎` 2회가 되는 순간:
  - `testEnabled=false`
  - 현재 암기 큐 뒤쪽의 동일 단어 복제본 전부 제거
  - 이후 걷기 암기 시작 시 `goodCount >= 2` 단어 기본 제외
- `지금 10분 공부` 프리셋에서도 졸업 단어 제외
- 보관함에서 `시험 넣기`를 누르면:
  - `testEnabled=true`
  - `goodCount=0`
  - 즉시 복습 가능 상태로 복귀
- BUILD 063


## v0.6.4 BUTTON UI
- `🎓 다음 시험부터 제외` 버튼 디자인 정리
- `컬렉션 / 잠김` 버튼을 pill 스타일로 통일
- 잠김 상태도 기본 브라우저 버튼처럼 보이지 않게 수정
- 기능 변경 없음 (UI만 개선)
- BUILD 064


## v0.6.5 API SAVER

API 비용 절약 패치.

### 테스트 채점
- 등록 뜻과 로컬에서 일치하면 API 호출 0회
- 같은 단어 + 같은 답을 다시 채점하면 이전 AI 판정 캐시 사용
- AI 판정 캐시는 최대 400개 보관
- AI가 필요한 애매한 답만 호출
- 채점 요청 출력량 축소
- 채점 API 자동 재시도 0회

### 사진 분석
- 같은 사진 + 같은 모드 + 같은 출처를 다시 분석하면 캐시 사용
- 최근 사진 분석 결과 최대 12세트 저장
- 휴대폰 원본을 그대로 보내지 않고 최대 1800px / 약 1.8MP로 축소
- JPEG 품질 0.88
- 웹 검색은 매번 강제하지 않음
- 철자/뜻 대응이 의심스러운 항목이 있을 때만 웹 검색하도록 지시
- 사진 출력 토큰 상한 6500 → 4600
- 사진 요청 재시도 2회 → 1회

### 오늘의 단어
- 기존 15개 선조회 + 5개씩 캐시 구조 유지
- API에 보내는 현재 단어 100개 → 취약 단어 35개
- 제외 목록 최대 350개 → 160개
- 출력 토큰 상한 3600 → 2600
- 재시도 2회 → 1회

### 내부 API 통계
- 실제 API 요청 횟수와 캐시 재사용 횟수를 로컬에 기록
- 비용 계산/청구 기능은 아니며 호출 절약 확인용 내부 기록

- BUILD 065
- 기존 단어장, 졸업, 리포트, 보상 기록 유지


## v0.6.6 BUTTON FIX
- `컬렉션 / 잠김 / 열기` 버튼을 기본 버튼 느낌이 안 나도록 전면 수정
- `다음 시험부터 제외` 버튼을 카드형 보조 액션 버튼으로 재디자인
- mobile Samsung 브라우저에서도 기본 버튼 스타일이 튀지 않도록 appearance reset 적용
- BUILD 066


## v0.6.7 PHOTO JSON FIX
- 여러 장 단어장 사진 분석에서 `Expected ',' or ']' after array element in JSON ...` 같은 오류가 날 때 자동 복구하도록 수정
- 1차 응답 JSON 파싱 실패 시:
  1) 빠른 JSON 복구 시도
  2) 그래도 실패하면 사진을 1장씩 안정 모드로 다시 읽어 병합
- 사진 추출 프롬프트를 더 짧고 보수적으로 조정해 JSON 깨짐 확률 감소
- 단어장 모드에서 불필요한 동의어/반의어/파생어/추가어 과다 생성을 제한
- BUILD 067


## v0.6.8 PHOTO ACCURACY
- 웹 검색 완료 여부와 저장 기능을 완전히 분리
- 직접 수정한 결과는 웹 검색 없이 바로 저장 가능
- 사진 인식 정확도 우선 모드:
  - 1800px / 1.8MP → 최대 2800px / 5MP
  - JPEG 0.88 → 0.94
  - 연필 손글씨 대비 약하게 강화
  - 여러 사진을 한 번에 섞어 읽지 않고 페이지별로 1장씩 분석
  - 왼쪽 영어 열 → 오른쪽 한국어 열 → 같은 행 재대조 순서 명시
  - 동의어/반의어/파생어/추가어 생성은 인식 단계에서 제거해 전사 정확도 우선
  - confidence 0.8 미만은 `⚠ 글씨 확인 필요` 표시
  - 사용자가 직접 수정하면 확인 경고 해제
- 이전 사진 인식 캐시(v1)는 사용하지 않고 v2 캐시로 새로 분석
- BUILD 068


## v0.6.9 FOLDER MANAGER
- 지문별 단어장을 실제 폴더 관리 방식으로 확장
- 새 폴더 생성
- 빈 폴더 유지
- 폴더 이름 변경
- 폴더 삭제
  - 폴더 삭제 시 단어 자체는 삭제하지 않고 `출처 미지정`으로 이동
- 폴더별 `단어 관리`
  - 여러 단어 체크
  - 전체 선택/해제
  - 선택한 단어를 다른 폴더로 일괄 이동
  - 선택한 단어 일괄 삭제
- 기존 sourceLabel들은 자동으로 폴더 목록에 등록
- 사진/직접입력으로 새 출처가 생기면 폴더 자동 생성
- 기존 단어/학습 기록 유지
- BUILD 069


## v0.7.0 HEADER + BUTTON ALIGN
- 상단 `설치` 버튼 삭제
- AI 상태 버튼만 남김
- 지문별 단어장 `이름 / 삭제` 버튼 동일 크기로 통일
- 모바일에서도 두 버튼 너비/높이 동일
- 기능 변경 없음
- BUILD 070


## v0.7.1 RANDOM + FOLDER TEST
- AI 랜덤 테스트 범위 선택:
  - `🎲 전체 랜덤`
  - `📁 폴더 선택`
- 전체 랜덤:
  - 시험졸업 제외 모든 단어
  - 모든 단어 1회씩
  - 완전 랜덤 순서
- 폴더 선택:
  - 원하는 폴더 지정
  - 해당 폴더의 시험졸업 제외 단어 전부 출제
  - 완전 랜덤 순서
- `새로`를 누르면 전체 순서를 다시 섞음
- 지문별 단어장의 `이 폴더 시험` → 해당 폴더 선택 모드로 바로 진입
- 기존 10문제 제한 없음
- shuffle을 Fisher-Yates 방식으로 교체해 랜덤 순서 품질 개선
- BUILD 071


## v0.7.2 FOLDER WALK REVIEW
- 걷기 암기 범위 선택:
  - `🚶 전체 암기`
  - `📁 폴더 선택`
- 폴더 선택 시 해당 폴더 단어만 걷기 암기
- `앎 2회` 졸업 단어는 기존처럼 제외
- 시험에서 `넘김`한 단어의 추가 등장 가중치는 선택한 폴더 안에서도 유지
- 지문별 단어장에 `🚶 이 폴더 암기` 추가
- 홈의 걷기 암기로 들어오면 기본값은 전체 암기
- `지금 10분 공부`는 전체에서 뽑은 위험단어 프리셋을 그대로 사용
- BUILD 072


## v0.7.3 FOLDER EDIT + BUTTON FIX
- 지문별 단어장의 `이름 / 삭제` 버튼을 동일한 고정 영역에 배치
  - 같은 Y 위치
  - 같은 너비/높이
  - 모바일에서도 줄바꿈/밀림 방지
- `단어 관리` 목록에 각 단어별 `수정` 버튼 추가
- 단어 수정:
  - 영어 단어/표현 수정
  - 한국어 뜻 수정
  - 여러 뜻은 `/` 또는 `;`로 구분
  - 기존 학습 기록(앎/오답/넘김/시험졸업 등)은 유지
  - 다른 단어와 영어 철자가 중복되면 저장 차단
- BUILD 073


## v0.7.4 COST CUT
### 오늘의 단어 완전 삭제
- 홈 카드 삭제
- 전용 화면 삭제
- 웹 검색 추천 호출 삭제
- 오늘의 단어 캐시/상태 코드 삭제
- 과거에 이미 단어장에 저장된 단어는 유지

### 사진 API 비용 절약
- 모든 사진 1차 인식: `gpt-5.6-luna`
- 아래 경우에만 해당 사진 1장 `gpt-5.6-terra` 정밀 재검사:
  - confidence < 0.82
  - warnings 존재
  - 결과가 비어 있거나 필수 필드 누락
- 사진 API 자동 재시도: 0회
- Structured Outputs(JSON Schema) 사용
  - JSON 형식 깨짐으로 인한 별도 AI 복구 호출 제거
- 사진 캐시는 v3로 갱신
- 같은 사진은 캐시 재사용

### 기타
- 일반 의미 테스트의 애매한 답만 Luna 사용 + 기존 판정 캐시 유지
- 기존 단어/폴더/졸업/학습 기록 유지
- BUILD 074


## v0.7.5 API METER + 150 PHOTO LIMIT
- 월 150장 사진 AI 분석 제한
- 캐시 재사용 사진은 한도 차감 0
- Luna 1차 분석 성공 시 사진 1장 차감
- Terra 재검사는 같은 사진이라 추가 장수 차감 없음
- 월 변경 시 자동 초기화
- Responses API 실제 usage의 input/output/cached/cache-write token 추적
- Luna / Terra별 호출 수와 예상 비용 표시
- API 모달에서 이번 달 총 예상 OpenAI 비용 확인
- 현재 제한은 localStorage 기반. 상용화 시 서버 계정별 제한 필수
- BUILD 075


## v0.7.6 PRIVATE BETA
- 친구 2~3명 배포용 Vercel 서버 프록시 추가
- 브라우저 OpenAI API key 완전 제거 + 구버전 localStorage key 자동 삭제
- 초대코드 인증 (`BETA_INVITE_CODES`)
- 자동 모델: GPT-5.6 Luna only
- GPT-5.6 Sol: 0 / 코드에서 제거
- GPT-5.6 Terra: 사용자가 `정밀 재검사` 버튼을 눌렀을 때만
- 서버는 OpenAI request model을 직접 강제해 클라이언트가 Sol/Terra를 임의로 선택할 수 없게 제한
- OpenAI `store:false` 강제
- 사진 1회 요청당 1장, 출력 토큰 상한 적용
- 이미지 준비 크기 2000px / 3MP / JPEG 0.84로 비용·전송량 절감
- Vercel 배포용 `api/`, `package.json`, `vercel.json`, `.env.example` 포함
- BUILD 076


## v0.7.7 — GitHub Pages + Minimal Worker
- 기존 GitHub Pages 유지
- Vercel 파일/서버 제거
- Cloudflare Worker 단일 파일 `worker.js` 추가
- Worker URL은 앱 설정에서 1회 입력
- `?server=` 링크를 통해 친구 폰에 Worker 주소 자동 설정
- Luna 자동 / Terra 수동 / Sol 서버에서 선택 불가
- OpenAI API key는 Worker Secret에만 저장


## BUILD 078 — invite connection hotfix
- GitHub Pages → Worker 초대코드 연결 요청을 `text/plain` 본문 방식으로 변경
- 커스텀 인증 헤더 없이도 연결되어 모바일 CORS/preflight 문제를 회피
- Worker는 새 본문 방식 + 기존 헤더 방식 둘 다 허용
- Worker 루트 상태 화면에 `invite_count`, `openai_configured` 추가
- 자동 AI 모델은 Luna만, Terra는 수동 precision 요청만


## BUILD 079
- 테스트 영→한 AI 채점 오류 수정: Worker가 문자열 `input`도 허용
- JSON 복구 등 텍스트형 AI 요청도 정상 처리
- 사진 인식 화면의 이름 입력을 폴더 이름으로 명확화
- 이름을 입력한 상태에서 사진 인식 성공 시 해당 폴더 즉시 자동 생성
- 추출된 모든 단어/추가 단어의 `sourceLabel`을 해당 폴더로 강제 연결
- 캐시 결과 및 Terra 수동 재검사에도 동일한 폴더 규칙 적용


## BUILD 080
- 걷기 암기 `애매` 선택 후 동일 단어가 바로 연속 등장하는 문제 수정
- 넘김 가중치로 생기는 중복 카드를 다른 단어 뒤로 분산
- 테스트 정답 시 랜덤 칭찬 문구 표시
- 걷기 암기에서 `앎` 선택 시에도 짧은 칭찬 표시
- 테스트 `모르겠음 · 넘기기` 선택 시 정답을 약 1.6초 보여준 후 자동으로 다음 문제 이동
- `모르겠음`은 기존처럼 오답률에는 포함하지 않고 복습 우선순위만 높임
- Cloudflare Worker 변경 없음


## BUILD 081 — English / Minor-language profiles
- 최상단에서 영어 모드 ↔ 부전공어 모드 전환
- 부전공어: 러시아어 / 일본어 / 프랑스어 / 중국어
- 5개 언어 모두 단어, 폴더, 학습기록, 테스트 큐, 보상, AI 채점 캐시, 사진 캐시를 언어별로 분리
- 기존 BUILD 080 이하 영어 데이터는 영어 모드에 그대로 유지
- 부전공어 사진 인식은 문자 단위 2중 대조 + 언어별 철자 규칙 적용
  - 러시아어: 키릴 문자, е/ё, ь/ъ, 어미, 강세 표시 보존
  - 일본어: 한자/히라가나/가타카나, 작은 글자, 장음, 탁점 보존
  - 프랑스어: 악센트, ç, apostrophe, hyphen 보존
  - 중국어: 간체/번체 비변환, 유사 한자 획 대조, 병음은 사진에 있을 때만 보존
- 부전공어의 confidence 경고 기준을 더 엄격하게 표시
- TTS 발음을 현재 언어(ru-RU/ja-JP/fr-FR/zh-CN)에 맞게 전환
- 테스트 방향과 입력 문구도 현재 언어에 맞게 자동 변경
- 백업 파일에 studyLang 기록, 다른 언어 백업을 잘못 가져올 때 경고
- Cloudflare Worker 변경 없음 (BUILD 079 Worker 그대로 사용)
